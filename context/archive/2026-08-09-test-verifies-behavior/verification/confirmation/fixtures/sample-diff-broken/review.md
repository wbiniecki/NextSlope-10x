# NextSlope code review

**Blocked** — 4 blocking findings at or above `high`, out of 5 findings.

## Blocking reasons

- critical: flyway-forward-only at src/main/resources/db/migration/V3__create_preference_profiles.sql:7 — V3__create_preference_profiles.sql is an already-applied migration; adding the display_nickname column here edits it in place instead of introducing a new V{n+1} migration. On Neon's free tier this cannot be corrected once applied.
- high: constructor-injection at src/main/java/com/nextslope/profile/PreferenceProfileService.java:30 — New field `@Autowired private ProfileAuditRepository profileAuditRepository` on a production @Service introduces field injection where the class already uses constructor injection via Lombok; it should be added as a private final constructor parameter instead.
- high: ddl-auto-validate at src/main/resources/application.properties:15 — spring.jpa.hibernate.ddl-auto changed from validate to update, defeating the fail-fast boot-time schema check across all profiles that inherit this file and allowing Hibernate to silently mutate the schema going forward.
- high: flyway-forward-only at src/main/resources/db/migration/V6__create_profile_audit.sql:6 — `id SERIAL PRIMARY KEY` is Postgres-specific and not the portable `BIGINT GENERATED BY DEFAULT AS IDENTITY` idiom used elsewhere in the repo; this migration is reachable by both engines during normal test/prod runs and risks failing to apply identically on H2 vs. Postgres.

## Criterion scores

| Criterion | Score | Justification |
| --- | --- | --- |
| `flyway-forward-only` | 1/10 | V3__create_preference_profiles.sql, an already-applied migration, is directly edited to add a display_nickname column — this is the textbook 'edit an applied migration' violation the rule exists to prevent, and Neon's free tier cannot roll it back. Separately, the new V6__create_profile_audit.sql uses `id SERIAL PRIMARY KEY`, which is Postgres-only DDL that will not parse the same way on H2 in the way the repo's portable idiom (`BIGINT GENERATED BY DEFAULT AS IDENTITY`) does, breaking dual-engine portability for a brand-new file. |
| `ddl-auto-validate` | 2/10 | spring.jpa.hibernate.ddl-auto is changed from validate to update in application.properties, exactly the change the rule calls out by name, with a comment openly stating the intent to let Hibernate mutate the schema instead of adding a migration. This defeats the fail-fast boot-time check across every profile that shares this file. |
| `constructor-injection` | 2/10 | PreferenceProfileService, an existing @Service that already uses `private final` fields with an implicit @RequiredArgsConstructor, gains a new `@Autowired private ProfileAuditRepository profileAuditRepository` field. This is precisely the field-injection pattern AGENTS.md says appears nowhere in src/main/java — it should instead be a private final constructor parameter. |
| `access-control-scoping` | — | The diff touches migrations, a service field, an application property, and an e2e test timing statement; it introduces no request path/parameter/body identifier, no repository ownership predicate change, and no security permit-list change, so this criterion has nothing to govern here. |
| `e2e-conventions` | 4/10 | A fixed `page.waitForTimeout(500)` is inserted into HtmxSmokeE2eTests between a click and the subsequent assertions, directly contradicting the stated rule that the suite contains no sleeps and that waits must be assertion- or condition-based. The surrounding getByRole-style interactions and structural assertions elsewhere in the file are untouched and remain compliant. |
| `test-verifies-behavior` | 9/10 | The only test change is the inserted sleep; the pre-existing `assertThat(toggle).hasAttribute(...)` and `.hasText(...)` assertions that verify the toggle behavior remain intact and unweakened immediately afterward, so the test can still fail if the behavior regresses. No assertion was removed, softened, or made unreachable. |

Scores are diagnostic. Only findings at or above the fail-on severity block the change.

## Findings

### `src/e2eTest/java/com/nextslope/e2e/HtmxSmokeE2eTests.java`

- **line 229** · medium · `e2e-conventions` — Added `page.waitForTimeout(500)` is a fixed sleep, which the e2e conventions explicitly forbid; the wait for the swap to settle should be expressed as an assertion-based or `waitForFunction` condition instead.

### `src/main/java/com/nextslope/profile/PreferenceProfileService.java`

- **line 30** · high · `constructor-injection` — New field `@Autowired private ProfileAuditRepository profileAuditRepository` on a production @Service introduces field injection where the class already uses constructor injection via Lombok; it should be added as a private final constructor parameter instead.

### `src/main/resources/application.properties`

- **line 15** · high · `ddl-auto-validate` — spring.jpa.hibernate.ddl-auto changed from validate to update, defeating the fail-fast boot-time schema check across all profiles that inherit this file and allowing Hibernate to silently mutate the schema going forward.

### `src/main/resources/db/migration/V3__create_preference_profiles.sql`

- **line 7** · critical · `flyway-forward-only` — V3__create_preference_profiles.sql is an already-applied migration; adding the display_nickname column here edits it in place instead of introducing a new V{n+1} migration. On Neon's free tier this cannot be corrected once applied.

### `src/main/resources/db/migration/V6__create_profile_audit.sql`

- **line 6** · high · `flyway-forward-only` — `id SERIAL PRIMARY KEY` is Postgres-specific and not the portable `BIGINT GENERATED BY DEFAULT AS IDENTITY` idiom used elsewhere in the repo; this migration is reachable by both engines during normal test/prod runs and risks failing to apply identically on H2 vs. Postgres.
