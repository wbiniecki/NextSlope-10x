# NextSlope code review

**Blocked** — 4 blocking findings at or above `high`, out of 6 findings.

## Blocking reasons

- critical: access-control-scoping at src/main/java/com/nextslope/web/ProfileController.java:45 — New `GET /profile/{userId}` handler loads the profile via `preferenceProfileService.loadFormForUser(userId)` using a client-supplied path variable instead of resolving ownership through `CurrentUserService.requireUserId(principal)`, opening an IDOR on a resource family that previously had no id in the path.
- critical: ddl-auto-validate at src/main/resources/application.properties:16 — `spring.jpa.hibernate.ddl-auto` is changed from `validate` to `update`, explicitly to avoid writing a correct migration for a new entity. This disables the fail-fast schema check the repo relies on in every profile.
- critical: flyway-forward-only at src/main/resources/db/migration/V3__create_preference_profiles.sql:7 — This diff has no `new file mode` header for V3, meaning it edits an already-applied migration to add `display_nickname VARCHAR(64)`. Applied migrations are immutable; this column addition belongs in a new V{n+1}__ file.
- high: constructor-injection at src/main/java/com/nextslope/profile/PreferenceProfileService.java:31 — New field `profileAuditRepository` uses field `@Autowired` and is not `final`, unlike the other collaborators in this @Service which rely on Lombok's @RequiredArgsConstructor. This reintroduces field injection where constructor injection is the repo's sole convention.

## Criterion scores

| Criterion | Score | Justification |
| --- | --- | --- |
| `flyway-forward-only` | 2/10 | The diff modifies V3__create_preference_profiles.sql, an already-applied migration, by adding a display_nickname column — corrections to applied migrations must go in a new V{n+1} file instead. Separately, the new V6__create_profile_audit.sql uses `SERIAL PRIMARY KEY`, which is Postgres-specific and does not parse on H2 in the repo's portable idiom (`BIGINT GENERATED BY DEFAULT AS IDENTITY`), breaking dual-engine portability. |
| `ddl-auto-validate` | 2/10 | application.properties changes `spring.jpa.hibernate.ddl-auto` from `validate` to `update`, explicitly to let Hibernate auto-apply columns for a new entity instead of using a migration. This is precisely the mutation-permitting mode the repo forbids, and the inline comment even documents the workaround being taken. |
| `constructor-injection` | 2/10 | PreferenceProfileService, a @Service under src/main/java, gains `@Autowired private ProfileAuditRepository profileAuditRepository;` — a field-injected, non-final collaborator — alongside existing constructor-injected final fields where extending the (implied) @RequiredArgsConstructor would have sufficed. |
| `access-control-scoping` | 1/10 | A new `GET /profile/{userId}` endpoint takes an id straight from the URL path and passes it directly to `loadFormForUser(userId)`, `hasProfile(userId)`, bypassing `CurrentUserService.requireUserId(principal)` entirely. This is the canonical IDOR pattern the criterion calls out almost verbatim, on a route family (`/profile`) the repo's own Javadoc says has no cross-user surface today. |
| `e2e-conventions` | 3/10 | A fixed `page.waitForTimeout(500)` is inserted into HtmxSmokeE2eTests.java to let an HTMX swap 'settle' — exactly the sleep-based wait the convention prohibits in favor of assertion-based auto-waiting (the subsequent `assertThat(toggle).hasAttribute(...)` would have waited on its own). The rest of the file's conventions (locators, teardown, seed data) are untouched by this diff. |

Scores are diagnostic. Only findings at or above the fail-on severity block the change.

## Findings

### `src/e2eTest/java/com/nextslope/e2e/HtmxSmokeE2eTests.java`

- **line 230** · medium · `e2e-conventions` — `page.waitForTimeout(500)` is a fixed sleep added before re-reading the toggle button; waits in this suite must be assertion-based (e.g. letting the following `assertThat(toggle).hasAttribute(...)` auto-wait) or condition-based via `page.waitForFunction`.

### `src/main/java/com/nextslope/profile/PreferenceProfileService.java`

- **line 31** · high · `constructor-injection` — New field `profileAuditRepository` uses field `@Autowired` and is not `final`, unlike the other collaborators in this @Service which rely on Lombok's @RequiredArgsConstructor. This reintroduces field injection where constructor injection is the repo's sole convention.

### `src/main/java/com/nextslope/web/ProfileController.java`

- **line 45** · critical · `access-control-scoping` — New `GET /profile/{userId}` handler loads the profile via `preferenceProfileService.loadFormForUser(userId)` using a client-supplied path variable instead of resolving ownership through `CurrentUserService.requireUserId(principal)`, opening an IDOR on a resource family that previously had no id in the path.

### `src/main/resources/application.properties`

- **line 16** · critical · `ddl-auto-validate` — `spring.jpa.hibernate.ddl-auto` is changed from `validate` to `update`, explicitly to avoid writing a correct migration for a new entity. This disables the fail-fast schema check the repo relies on in every profile.

### `src/main/resources/db/migration/V3__create_preference_profiles.sql`

- **line 7** · critical · `flyway-forward-only` — This diff has no `new file mode` header for V3, meaning it edits an already-applied migration to add `display_nickname VARCHAR(64)`. Applied migrations are immutable; this column addition belongs in a new V{n+1}__ file.

### `src/main/resources/db/migration/V6__create_profile_audit.sql`

- **line 6** · medium · `flyway-forward-only` — `id SERIAL PRIMARY KEY` is Postgres-specific and not portable to H2's PostgreSQL-compatibility mode in the way the repo's other migrations are. Use the established portable idiom `BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY` instead.
