<!-- IMPL-REVIEW-REPORT -->
# Implementation Review: Mark Resorts as Visited (S-04)

- **Plan**: context/changes/mark-visited/plan.md
- **Scope**: Phase 1 of 3 (Persistence — visited_resorts table, entity, repository)
- **Date**: 2026-06-26
- **Commit reviewed**: 078ffad
- **Verdict**: APPROVED
- **Findings**: 0 critical, 0 warnings, 1 observation

## Verdicts

| Dimension | Verdict |
|-----------|---------|
| Plan Adherence | PASS |
| Scope Discipline | PASS |
| Safety & Quality | PASS |
| Architecture | PASS |
| Pattern Consistency | PASS |
| Success Criteria | PASS |

## Plan adherence detail

- **V4__create_visited_resorts.sql** — MATCH. `id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY`,
  `user_id`/`resort_id` `BIGINT NOT NULL`, `created_at TIMESTAMP NOT NULL`, `CONSTRAINT
  uq_visited_user_resort UNIQUE (user_id, resort_id)`, no `updated_at`. DDL style mirrors V1–V3.
- **VisitedResort entity** — MATCH. Lombok `@Getter/@Setter/@NoArgsConstructor/@AllArgsConstructor/@Builder`,
  `Long id` IDENTITY, `Long userId`/`Long resortId` non-null columns, `Instant createdAt`
  `@CreationTimestamp` (`nullable=false, updatable=false`), no `@UpdateTimestamp`.
- **VisitedResortRepository** — MATCH. `existsByUserIdAndResortId`, `@Modifying @Transactional long
  deleteByUserIdAndResortId`, `@Query("select v.resortId ...") Set<Long> findResortIdsByUserId`.
- **Tests** — MATCH. H2 `@DataJpaTest` (`VisitedResortRepositoryTests`) covers mark/exists,
  `@CreationTimestamp` population, unique-constraint `DataIntegrityViolationException`, delete + no-op,
  per-user id projection. `VisitedResortRepositoryPostgresTests` (`@Testcontainers`) proves migration +
  queries on real Postgres. Both mirror existing `*RepositoryTests` / `*RepositoryPostgresTests`.

## Success criteria

- Automated: `VisitedResortRepositoryTests`, `VisitedResortRepositoryPostgresTests`,
  `NextslopeApplicationTests`, and full `./gradlew test` — all green. No lint errors.
- Manual (1.5, H2 console): confirmed by the developer.

## Findings

### F1 — No DB foreign-key constraints on user_id / resort_id

- **Severity**: 🔭 OBSERVATION
- **Impact**: 🏃 LOW — quick decision; fix is obvious and narrowly scoped
- **Dimension**: Architecture
- **Location**: src/main/resources/db/migration/V4__create_visited_resorts.sql:1
- **Detail**: `visited_resorts` stores `user_id`/`resort_id` as plain BIGINTs with no FK constraint,
  whereas V3 `preference_profiles` carries `fk_preference_profiles_user_id → users(id)`. The integrity
  anchor here is only `uq_visited_user_resort`. This is an explicit plan decision (plan lines 73-77),
  so it MATCHES the plan — flagged only for traceability: orphan rows are theoretically possible, but
  resorts soft-delete (active flag) and S-07 owns the account-deletion cascade.
- **Fix**: None required — accept as the documented plan decision.
- **Decision**: ACCEPTED (documented plan decision)
